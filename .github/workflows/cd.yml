name: CD Pipeline

on:
  # Manual trigger only; CD is disabled for now.
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: CD placeholder
        run: echo "CD pipeline is currently disabled for this repo."
