name: CD Pipeline

on:
  # Manual trigger only, and right now this is just a placeholder
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: CD placeholder
        run: echo "CD pipeline is currently disabled for this repo."
